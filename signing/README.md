# Stable sideload signing for ChEng AIO Android APKs.
# Same keystore + higher versionCode = install over the previous APK (no uninstall).
# Password default: chengpro (override with CHENGPRO_KEYSTORE_PASS).
